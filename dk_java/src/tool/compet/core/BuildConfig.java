package tool.compet.core;

/**
 * This acts as build config, but created manually.
 */
public class BuildConfig {
	/**
	 * Change value to false when release the app.
	 */
	public static final boolean DEBUG = true;
}
